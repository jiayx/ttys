{
  "name": "ttys",
  "private": true,
  "packageManager": "pnpm@10.18.0",
  "scripts": {
    "dev": "pnpm --filter @ttys/web run dev",
    "build": "pnpm --filter @ttys/web run build",
    "deploy": "pnpm --filter @ttys/web run deploy"
  }
}
